# Token Saving Replay Agent Workspace

This repository now keeps the browser product in a dedicated subfolder:

- `dual-model-local-chat-with-env-patcher/` - browser-first FastAPI + TypeScript local chat with environment patching.

The VS Code extension has been split into a separate local Git repository:

- `C:\Users\Euu\Desktop\Projects\extension-token-saving-elephant-memory`

The two products intentionally duplicate backend code for now so they can be released and stabilized independently.
